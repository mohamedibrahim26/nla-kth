# Core ML stack (Colab already has torch; listed for local/reproducibility)
torch>=2.1
transformers>=4.44
datasets>=2.19
accelerate>=0.30
peft>=0.11           # LoRA adapters (used in the warm-start / RL phases)
bitsandbytes>=0.43   # 4-bit loading for the (larger) teacher model

# Numerics, data, plotting
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
tqdm>=4.66
